## 九流WP助手 2.5.0

本版本新增第六功能域 **“下载页美化”**，将此前维护的子比资源下载页方案正式集成到九流WP助手统一后台。

### 新增内容

- 后台可直接选择 **科技蓝紫版、简洁商务版、VIP 资源中心版** 三套风格。
- 左上角封面支持五种策略：
  - 自动模式
  - 固定特色图片
  - 固定正文首图
  - 固定文章随机图
  - 固定默认图片
- 自动模式依次回退：**特色图 → 正文首图 → 稳定随机图 → 默认图**。
- 随机图按文章稳定选择，同一文章刷新后不会反复变化。
- 正文取图兼容 `src`、`data-src`、`data-original`、`data-lazy-src`，并过滤头像、表情、Logo、图标、占位图和跟踪像素。
- 默认图可从 WordPress 媒体库选择；留空时使用插件内置 SVG。

### 兼容与安全

- 通过插件级请求接管实现，不修改 `/wp-content/themes/zibll/pages/download.php`。
- 功能关闭、子比接口缺失或请求不符合资源下载条件时，自动保留子比原页面。
- 支付状态、会员权限、免费额度和下载按钮继续调用子比原生函数，不重写交易逻辑。
- 正式安装包附带 SHA-256 文件，供九流WP助手后台更新器校验。
