# 九流WP助手 2.5.2

> 一个真实的单体 WordPress 插件，不是多个插件的压缩集合。

九流WP助手把页面美化、反调试保护、沉浸式预加载、AI 文章摘要和媒体链接管理设计成六个内部功能域，共用一个插件生命周期、一个后台应用、一个菜单树和一个安全更新器。

## 统一架构

- **一个插件头**：只有根目录 `jiuliu-wp-assistant.php` 会被 WordPress 识别为插件。
- **一个激活入口**：中央功能注册表统一初始化默认设置与迁移。
- **一个菜单中心**：总览、六项功能和系统更新全部由统一后台控制器注册。
- **一个视觉系统**：顶栏导航、页面标题、卡片、表单、选项卡、按钮和状态提示使用同一设计语言。
- **一个更新源**：只从 `nljie1103/wp-assistant` 更新，支持版本、SHA-256、完整备份与失败回滚。

内部按功能域拆分文件，是为了代码清晰和维护安全，并不代表这些功能仍然独立运行。

## 功能

### 页面美化

樱花、雪花、落叶、气泡、只在页脚上方出现的底部波浪、粒子、灯笼、多种鼠标轨迹、右键菜单、分级内容保护、复制版权、背景音乐、欢迎弹窗与自定义 CSS/JavaScript。

### 反调试保护

支持平衡、严格和自定义检测模式，并提供四级可组合防御链。各层可独立选择遮罩、模糊、DOM 替换、Console 清理、历史锁定、死循环刷新、循环关闭、无限 Debugger、页面硬锁和会话持久锁；所有高风险动作默认关闭并提供紧急旁路。

### AI 文章摘要

支持多服务商和模型、按模型绑定 API Key、文章元数据缓存、外观预设、透明装饰图、文字动画、编辑器快捷生成，以及每 IP/全站双重额度保护。

### 沉浸式预加载

支持八种沉浸开屏、自定义 Logo、DOM/两帧绘制/完整加载三种结束策略、最短与最长时长、减少动态、节省流量跳过、会话策略与移动端适配。

### 媒体与链接

支持媒体相对地址、安全入口域名白名单、明确反向代理源站、历史内容扫描、预览与修复、响应头检测和 SEO 辅助。

### 下载页美化

内置科技蓝紫、简洁商务、VIP 资源中心三套子比资源下载页模板；支持特色图、正文首图、稳定随机图、默认图和自动回退，不修改主题文件。

## 数据兼容

2.0 继续读取旧版设置键，因此从独立插件或 1.x 套件迁移后，不需要重新填写 API Key、动画参数、Logo、域名规则和扫描配置。

迁移前请先停用以下独立插件，避免前台重复输出：

- 九流页面美化特效
- 九流 AI 文章摘要
- 九流沉浸式预加载
- 九流媒体相对地址

## 目录结构

```text
wp-assistant/
├── jiuliu-wp-assistant.php
├── includes/
│   ├── class-jlwa-feature-registry.php
│   ├── class-jlwa-admin.php
│   └── class-jlwa-updater.php
├── assets/
│   ├── css/admin.css
│   └── js/admin.js
├── features/
│   ├── page-effects/
│   ├── anti-debug/
│   ├── ai-article-summary/
│   ├── immersive-preloader/
│   ├── relative-media-urls/
│   └── download-page.php
├── readme.txt
└── uninstall.php
```

## 安装

1. 下载仓库 ZIP。
2. WordPress 后台进入“插件 → 安装插件 → 上传插件”。
3. 启用“九流WP助手”。
4. 从后台左侧菜单进入统一控制台。

## 安全更新

在线更新优先读取 GitHub 最新正式 Release，并校验整个安装 ZIP 的 SHA-256；Release 接口不可用时才回退主分支。随后执行版本/主文件校验、当前插件目录完整备份、清理旧目录、安装新版本和安装后复核，失败自动恢复旧版本。

## 许可证

GPLv2 or later。


## 2.0.1 后台界面修复

统一加载四项功能的后台资源，并重做功能正文的卡片、表单、选项卡与响应式排版。


## 2.0.2 热修复

- 修复 2.0.0 在线更新仍要求旧 modules 文件的问题。
- 增加不执行的兼容占位文件，让旧版本可以跨架构升级。


## 2.1.0 功能与稳定性升级

- 页面美化加入内容保护、复制版权、复制提示、落叶、气泡、底部波浪和更多鼠标轨迹。
- AI 摘要加入透明装饰图、访客 API 额度保护、正文成本限制和文章元数据缓存。
- 预加载改为性能优先的真实完成策略，并完整清理动画和监听器。
- 多域名模块移除任意 Host 信任，增加明确源站与安全诊断。
- 更新器优先安装正式 Release，并校验完整 ZIP。


## 2.1.1 内容保护边界修正

- 公开网页的开发者工具和 `view-source:` 无法由网页脚本真正关闭。
- 新增服务器级全文访问限制、REST/RSS 隐藏和未授权 noindex，只有服务器不发送正文才属于真实访问控制。


## 2.2.0 独立反调试功能域

- 新增第五功能域“反调试保护”，拥有独立菜单、设置、前台脚本和触发日志。
- 引入多探测器评分、连续确认、分数衰减和探测器冷却，避免单一窗口变化直接误杀。
- 提供平衡、严格和自定义模式；严格模式启用窗口差值、Debugger 耗时和 Console 性能探测。
- 检测后支持只记录、遮罩模糊、替换正文、跳转和可选关闭尝试。
- 支持关闭调试环境后自动恢复、管理员和登录用户绕过、页面范围与路径排除。
- 触发日志只保存匿名访客指纹，不保存原始 IP。


## 2.3.0 分级防御与页脚波浪修正

- 反调试保护新增四级独立防御链，每一级都可单独设置动作和触发延迟。
- 加入可选的无限 Debugger、死循环刷新、循环关闭、页面硬锁和会话持久锁。
- 高风险动作默认关闭，并提供管理员绕过及带密钥的紧急旁路。
- 后台只显示九流WP助手主版本，不再显示功能域内部版本。
- 底部波浪改为插入页脚之前的普通文档流元素；子比主题默认定位 `footer.footer`，滚动到底部才可见。


## 2.3.1 预加载与宽屏后台修复

- 补齐星轨、电影快门和水墨预览资源。
- 开屏生命周期从第一帧开始计时，增加首屏关键资源策略和独立硬超时。
- 优化水墨滤镜、星轨毛玻璃和退出合成性能。
- 后台移除固定 1380px 宽度并增加完整画布背景。
