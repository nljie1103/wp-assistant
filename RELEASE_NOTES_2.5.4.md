## 九流WP助手 2.5.4

本版本修复子比下载页美化的两处封面逻辑问题。

### 热门资源图片
- 热门资源不再只读取特色图片。
- 没有特色图时，按后台已选择的封面策略继续读取正文首图、稳定随机图和默认图。
- 自动模式与左上角主封面保持一致：特色图 → 正文首图 → 正文/附件稳定随机图 → 默认图。
- 过滤头像、表情、Logo、图标、加载占位图和透明像素。

### 左上角主封面比例
- 图片加载后读取真实 `naturalWidth` 和 `naturalHeight`，不再只依赖媒体库附件尺寸。
- 正文外链图、懒加载图以及无法映射附件 ID 的图片也能正确识别横图、方图和竖图。
- 竖图时整个封面容器切换为竖向真实比例，不再把竖图放进横向方框。
- 三套模板同时生效：科技蓝紫版、简洁商务版、VIP 资源中心版。

发布包同时包含服务端热门资源封面回退与浏览器端真实尺寸校正。
