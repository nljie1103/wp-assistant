=== 九流WP助手 ===
Contributors: nljie1103
Tags: wordpress, anti-debug, devtools protection, ai summary, media urls, effects, preloader
Requires at least: 5.8
Tested up to: 6.6
Requires PHP: 7.4
Stable tag: 2.3.3
License: GPLv2 or later
License URI: https://www.gnu.org/licenses/gpl-2.0.html

一个真正统一的 WordPress 增强插件：页面美化、反调试保护、沉浸式预加载、AI 文章摘要、媒体与多域名链接管理。

== Description ==

九流WP助手不是把多个旧功能放进同一个目录，而是重新设计成一个完整插件：

* WordPress 只识别一个插件头、一个激活入口和一个更新源。
* 所有后台菜单、页面标题、导航、状态提示和视觉组件由统一控制台负责。
* 五项能力作为内部功能域启动，不再注册各自的插件生命周期、顶级菜单或更新器。
* 保留成熟业务逻辑与旧设置键，升级后无需重新填写 API、动画参数、域名规则和 Logo。
* 在线更新先校验版本和 SHA-256，再完整备份插件目录；失败自动回滚。

五项内置能力：

1. 页面美化：樱花、雪花、落叶、气泡、仅在页脚上方出现的底部波浪、粒子、鼠标轨迹、右键菜单、内容保护、复制版权、背景音乐和自定义代码。
2. 反调试保护：多探测器评分、连续确认、四级可组合防御链，以及可选的持续 Debugger、循环刷新、循环关闭、会话锁定和紧急旁路。
3. AI 文章摘要：多服务商模型、按模型保存 API Key、文章元数据缓存、装饰图标/透明图片、动画样式、编辑器操作和访客额度保护。
4. 沉浸式预加载：八种开屏效果、自定义 Logo、首屏关键资源策略、从第一帧开始计时的硬超时、减少动态效果和动画资源清理。
5. 媒体与链接：媒体相对地址、安全域名白名单、明确源站、反向代理诊断、扫描预览、安全修复和 SEO 辅助。

== Installation ==

1. 停用旧的四个独立插件，避免重复输出。
2. 上传九流WP助手插件目录或 ZIP。
3. 启用“九流WP助手”。
4. 从后台左侧“九流WP助手”进入统一控制台。

旧设置会继续沿用；本插件不会在升级时删除现有文章、媒体、AI 缓存或配置。

== Changelog ==

= 2.3.3 =
* 修复会话标记在开屏真正显示前就被写入，导致预加载永久被跳过的问题。
* 会话标记改为版本化，并在开屏节点完成首帧渲染后才写入。
* 新增管理员“强制预览一次”，不受会话一次、减少动态和省流量规则影响。

= 2.3.2 =
* 修复开屏跳过点击穿透并立即结束动画。
* 默认关闭并迁移旧移动端反调试设置，避免 iPhone/iPad 误报。
* 新增浏览器端 iPadOS/触摸设备识别与移动端高风险探测独立开关。

= 2.3.0 =
* 反调试保护新增四级可组合防御链，每一级可独立设置启用状态、延迟和动作。
* 第一级支持警告遮罩、正文模糊、禁用交互和禁用选择。
* 第二级支持替换正文 DOM、循环清空 Console、锁定后退以及阻止复制/剪切。
* 第三级支持跳转、死循环刷新、循环关闭和会话持久锁。
* 第四级支持无限 Debugger 循环、高速清空 Console、刷新/关闭循环、页面硬锁和会话持久锁。
* 所有高风险动作默认关闭；新增管理员绕过和带密钥的紧急旁路地址。
* 后台不再显示各功能域独立版本号，只显示九流WP助手主版本。
* 底部波浪改为普通文档流元素，插入子比主题 `footer.footer` 之前，只在滚动到页面底部时出现。

= 2.2.0 =
* 新增第五功能域“反调试保护”，拥有独立菜单、设置、前台脚本和匿名触发日志。
* 新增平衡、严格和自定义模式；平衡模式默认避免高误判的窗口差值与 Debugger 耗时探测。
* 引入快捷键、Console 求值、页面调试库、窗口差值、Debugger 耗时、Console 性能和窗口失焦辅助探测器。
* 使用多信号评分、连续确认、命中窗口、分数衰减和探测器冷却，避免单一信号直接处置。
* 检测后支持只记录、遮罩并模糊正文、替换正文、跳转和可选关闭尝试。
* 支持调试环境关闭后自动恢复、移动端开关、管理员/登录用户绕过、页面范围与路径排除。
* 触发日志仅保存匿名访客摘要，不记录原始 IP，并带有频率限制和数量上限。
* 2.3.0 起将无限 debugger、死循环刷新和持续关闭作为默认关闭的可选高强度动作，并提供紧急旁路。

= 2.1.1 =
* 修正 Ctrl/Cmd+U 等页面内快捷键拦截，增加 keyCode 兼容和捕获阶段双重监听。
* 打印保护增加打印样式，即使从浏览器菜单打印也不会输出正文。
* 后台明确说明公开网页无法真正禁止浏览器开发者工具和 view-source:。
* 新增服务器级正文访问模式：可要求登录用户、作者/编辑或管理员才能取得完整文章/页面正文。
* 服务器保护可为未授权访客显示有限摘要，并隐藏 REST API、RSS/Atom 正文及输出 noindex/nofollow。

= 2.1.0 =
* 页面美化新增落叶、气泡、底部波浪与多种鼠标轨迹，并提高雪花/樱花数量上限。
* 重做前台动画调度：页面隐藏时暂停、移动端/节省流量自动降载、粒子碰撞改为空间网格，限制总动画对象预算。
* 新增分级内容保护：右键、选择、拖拽、打印、常见开发者快捷键和 Ctrl/Cmd+C/X 均可独立控制；管理员自动绕过。
* 新增复制版权模式：允许复制时可在前方、后方或两端添加自定义声明、当前页面链接、HTML 超链接与复制成功提示。
* AI 摘要新增内置装饰图标、媒体库透明图片、五种位置、尺寸/透明度/偏移设置。
* AI 摘要默认禁止游客实时消耗 API；新增每 IP 与全站小时额度、正文长度上限和生成锁。
* AI 永久缓存迁移至文章元数据，避免大量永久 transient 影响全站；自定义 API 改用安全远程请求。
* 沉浸式预加载新增星轨、电影快门和水墨效果，默认按 DOM/两帧绘制完成，不再隐藏正文或伪装精确网络进度。
* 预加载增加移动端、减少动态、节省流量、会话跳过和完整动画/监听器清理。
* 媒体与链接移除不安全的任意 Host 模式，自动信任数据库主域，新增明确源站 URL，并加固诊断请求与 Nginx 配置。
* 在线更新优先使用 GitHub 正式 Release，校验整个 ZIP 的 SHA-256；Release 不可用时才回退主分支。
* 保留 2.0.0 跨架构在线升级兼容标记，不会注册或执行旧模块。

= 2.0.2 =
* 修复 2.0.0/2.0.1 后台更新仍按旧 modules 架构校验，导致无法在线升级的问题。
* 更新包改为校验 2.x 的 features 架构、统一内容样式和中央注册表。
* 增加仅用于跨架构升级的空兼容占位文件；不会注册插件、不会执行旧模块。

= 2.0.1 =
* 修复功能页旧资源 hook 不匹配导致的正文无样式问题。
* 由统一后台强制调度四项功能的 CSS、JavaScript、媒体上传器和颜色选择器资源。
* 新增统一功能状态卡、内容画布、卡片、表单、选项卡和响应式布局。
* 重做页面美化特效卡片、AI 摘要设置、预加载效果网格和媒体扫描诊断界面。
* 增加未保存修改提示与选择卡片状态同步。

= 2.0.0 =
* 从四插件套件重构为真正的单插件架构。
* 仓库只保留一个 WordPress 插件头和一个激活入口。
* 新增中央功能注册表，统一启动四项内部功能。
* 所有菜单统一由一个后台控制器注册。
* 重做后台 UI：统一顶栏导航、控制台、功能卡片、表单、选项卡、状态与响应式布局。
* 移除四个功能各自的插件启动器、独立菜单、操作链接和更新器入口。
* 将 modules 目录重组为 features 功能域目录。
* 保持旧 option key 与缓存数据兼容。
* 保留安全更新器的完整备份、SHA-256 校验和失败回滚。

= 1.0.0 =
* 首次将四个独立插件放入同一个发布包。
