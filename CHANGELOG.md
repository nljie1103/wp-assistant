# 更新记录

本项目只使用 `main` 作为长期维护分支。每个正式版本都同步更新版本号、更新记录、Git 标签、GitHub Release 和可安装 ZIP。

## 2.3.1 — 2026-08-05

### 预加载预览与生命周期修复

- 补齐星轨、电影快门和水墨三个效果缺失的 SVG 预览资源，并增加图片失败时的后台降级预览。
- 开屏显示时间改为从 `<head>` 第一帧开始计算，不再等到主题页脚脚本执行后才启动最大等待计时。
- 预加载脚本改为 head 并行下载、defer 执行；关键内联逻辑提供独立硬超时，即使主题或第三方脚本阻塞也会按时解除遮罩。
- 新增“首屏关键资源”结束策略，只等待视口内少量图片与字体，并受最大时长限制，不等待页面底部图片、广告或统计脚本。
- 优化星轨中心毛玻璃、水墨全屏模糊和退出时整屏缩放，降低大屏 Edge/Chrome 的合成与滤镜开销。

### 后台宽屏布局与背景

- 移除统一后台固定 `1380px` 最大宽度，改为占用 WordPress 可用内容区域。
- 为宽屏后台增加渐变光晕和低对比度网格背景，超宽屏控制台可自动使用三列卡片。

## 2.3.0 — 2026-08-05

### 四级可组合反调试防御

- 新增四级防御链，每一级均可独立启用、设置延迟并组合多个动作。
- 第一级提供警告遮罩、正文模糊、禁用正文交互和禁用文本选择。
- 第二级提供替换正文 DOM、循环清空 Console、锁定后退以及阻止复制/剪切。
- 第三级提供跳转、死循环刷新、循环关闭和会话持久锁。
- 第四级提供无限 Debugger 循环、高速清空 Console、刷新/关闭循环、页面硬锁和会话持久锁。
- 所有高风险动作默认关闭；保留管理员绕过，并新增带密钥的紧急旁路地址。
- 旧版单一处置动作会在升级时迁移到对应防御层。

### 统一版本显示

- 后台控制台、功能页和系统页不再显示各功能域独立版本号。
- 功能域内部版本仅用于数据迁移和静态资源缓存；用户界面只显示九流WP助手主版本。

### 底部波浪定位修正

- 波浪不再使用 `position: fixed` 固定悬浮在浏览器底部。
- 改为插入真实页脚元素之前的普通文档流组件，只有滚动到页面底部时才能看到。
- 子比主题默认优先匹配 `footer.footer`，波浪位于友情链接和页脚内容上方；同时支持自定义页脚选择器。
- 重做为双层 SVG 横向循环波浪，避免旧旋转圆形波浪造成的悬浮水球观感。

## 2.2.0 — 2026-08-05

### 新增独立反调试功能域

- 新增第五功能域“反调试保护”，拥有独立后台菜单、配置页、前台脚本和触发日志，不再与页面美化配置混在一起。
- 新增平衡、严格和自定义三种运行模式。
- 平衡模式默认启用快捷键、Console 求值和页面调试库检测，关闭误判风险较高的窗口差值与 Debugger 耗时探测。
- 严格模式启用窗口差值、Debugger 耗时、Console 性能和窗口失焦辅助信号。
- 支持文章、独立页面、首页和归档范围控制，可排除指定路径。
- 支持管理员绕过、全部登录用户绕过和移动端单独开关。

### 判定与处置

- 使用多信号评分、连续确认、命中时间窗口、分数衰减和探测器冷却，避免单一窗口变化立即误杀。
- 窗口差值检测同时比较 `outerWidth/innerWidth`、`outerHeight/innerHeight`、固定像素阈值和视口比例。
- 检测后支持只记录、遮罩并模糊正文、替换正文区域、跳转和可选关闭尝试。
- 支持关闭调试环境后自动解除遮罩和恢复正文。
- 该版本尚未提供无限 `debugger`、死循环刷新或持续关闭；这些动作在 2.3.0 作为默认关闭的可选防御加入。

### 日志与隐私

- 新增公开访客触发日志接口，带 WordPress nonce、访客频率限制和日志数量上限。
- 只保存触发时间、信号、分数、相对页面路径、浏览器摘要和不可逆访客指纹，不保存原始 IP。

## 2.1.1 — 2026-08-05

- 修正 Ctrl/Cmd+U 等页面内快捷键捕获。
- 增加打印保护样式和服务器级正文访问限制。
- 支持隐藏未授权 REST API、RSS/Atom 正文以及输出 noindex/nofollow。
- 明确公开网页无法由页面脚本真正关闭浏览器菜单中的开发者工具或 `view-source:`。

## 2.1.0 — 2026-08-04

- 页面美化新增落叶、气泡、底部波浪、更多鼠标轨迹、分级内容保护和复制版权。
- AI 摘要新增透明装饰图、访客额度保护、正文成本限制和文章元数据缓存。
- 沉浸式预加载改为性能优先的真实完成策略，并完整清理动画资源。
- 媒体与多域名模块移除任意 Host 信任，增加明确源站和安全诊断。
- 更新器优先安装 GitHub 正式 Release，并校验完整 ZIP。

## 2.0.2 — 2026-08-04

- 修复后台能检测更新但仍按旧 `modules/` 目录检查，导致无法安装的问题。
- 增加无插件头、不会执行的跨架构兼容文件。

## 2.0.1 — 2026-08-04

- 修复统一功能页正文样式和脚本未加载的问题。
- 新增统一功能状态卡、内容画布、表单、选项卡和响应式布局。

## 2.0.0 — 2026-08-04

- 从四插件套件重构为真正的单插件架构。
- 只保留一个 WordPress 插件头、一个激活入口、一个菜单中心和一个更新源。
- 新增中央功能注册表并保留旧设置键兼容。

## 1.0.0 — 2026-08-04

- 首次把四个独立插件放入同一个发布包。
