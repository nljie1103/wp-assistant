# 更新记录

本项目只使用 `main` 作为长期维护分支。每个正式版本都必须同时完成版本号更新、更新记录、Git 标签、GitHub Release 和可安装 ZIP。

## 2.1.0 — 2026-08-04

### 页面美化

- 新增落叶、气泡、底部波浪和更多鼠标跟随样式。
- 提高樱花与雪花数量上限，并增加总动画对象预算。
- 页面进入后台标签页时暂停动画；移动端、减少动态效果和节省流量模式自动降载。
- 粒子连线改用空间网格，避免数量增大后出现近似 O(n²) 的性能问题。
- 新增分级内容保护，可分别控制右键、选择、拖动、打印、复制、剪切和常见开发者快捷键。
- 新增复制版权模式，可在复制内容前方、后方或两端追加自定义声明、原文地址和 HTML 链接。
- 新增复制成功提示；管理员和编辑页面自动绕过内容保护。

### AI 文章摘要

- 新增内置装饰图标和媒体库透明图片。
- 支持五种装饰位置、尺寸、透明度和偏移调整。
- 默认禁止游客实时消耗 AI API；新增每 IP 与全站小时额度。
- 增加文章正文发送长度上限、单文章并发锁和缓存二次检查。
- 永久摘要缓存迁移至文章元数据，避免大量永久 transient 影响全站性能。
- 自定义 API 改用安全 WordPress HTTP 请求。

### 沉浸式预加载

- 新增星轨、电影快门和水墨等效果。
- 默认按 DOM 就绪或连续两帧渲染完成，不再隐藏正文或伪装精确网络进度。
- 增加移动端、减少动态效果、节省流量、会话跳过和最长显示时间。
- 遮罩移除后完整清理计时器、事件监听器、画布和动画循环。

### 媒体与多域名

- 移除不安全的任意 Host 信任模式。
- 数据库原始主域自动进入可信域名，并新增明确源站 URL。
- 诊断请求改用安全 WordPress HTTP API。
- 修正 Nginx 示例，避免把入口 Host 原样传给源站以及产生代理循环。

### 更新系统

- 优先读取 GitHub 正式 Release，并校验整个 ZIP 的 SHA-256。
- Release 不可用时才回退主分支快照。
- 保留 2.0.0 跨架构升级兼容文件；这些文件无插件头且不会执行。

## 2.0.2 — 2026-08-04

- 修复 2.0.0/2.0.1 后台更新器仍按旧 `modules/` 目录检查，导致能够检测更新却无法安装的问题。
- 更新包改为验证 `features/`、统一内容样式和中央功能注册表。
- 增加仅用于完成一次跨架构升级的空兼容文件，不注册插件、不输出前台内容。
- 保留版本、主文件 SHA-256、完整目录备份和失败回滚。

## 2.0.1 — 2026-08-04

- 修复功能页沿用旧菜单 hook，导致正文 CSS/JavaScript 没有加载的问题。
- 由统一后台控制器调度四项功能的样式、脚本、媒体上传器和颜色选择器。
- 新增统一功能状态卡、内容画布、卡片、表单、选项卡和响应式布局。
- 重做页面美化特效卡片、AI 摘要设置、预加载效果网格和媒体扫描诊断界面。
- 增加未保存修改提示与选择卡片状态同步。

## 2.0.0 — 2026-08-04

- 从四插件套件重构为真实单插件架构。
- 仓库只保留一个 WordPress 插件头、一个激活入口和一个更新源。
- 新增中央功能注册表，统一启动页面美化、AI 摘要、沉浸式预加载和媒体链接管理。
- 所有菜单由一个后台控制器注册。
- 重做统一后台 UI：顶栏导航、总览、功能卡片、表单、选项卡、状态提示和响应式布局。
- 将旧 `modules/` 功能实现迁移到 `features/` 内部功能域。
- 移除四项功能各自的插件启动器、顶级菜单、操作链接和更新器。
- 保留旧 option key 与缓存数据，升级无需重新填写现有配置。

## 1.0.0 — 2026-08-04

- 首次把四个独立插件放入同一个发布包。
- 该版本仍属于套件式组合，后续由 2.0.0 完成真正的单插件重构。
