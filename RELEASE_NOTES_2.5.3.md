## 九流WP助手 2.5.3

本版本修复页面美化后台无法保存，以及 AI 摘要部分入场动画不播放或文字被隐藏的问题。

### 页面美化
- 修复多个历史小数默认值与 HTML `step` 不一致，导致浏览器阻止整张表单提交。
- 未启用的特效不再影响其他设置保存。
- 小数参数允许按实际值提交，最终范围仍由插件服务器端安全清洗。

### AI 文章摘要
- 重做动画播放兜底，入场动画使用可靠的浏览器动画接口。
- 修复部分动画没有明显入场效果的问题。
- 修复逐行渐入在动画失败时文字永久透明的问题。
- 打字机动画增加完整文字恢复保护，执行异常也不会留下空白卡片。
- 系统开启“减少动态效果”时自动直接显示完整摘要。
- 动画失败时只退化为无动画，摘要文字始终可见。
